# aspauldingcode

Personal site is Astro plus React islands on Vercel. Read `content/work/*.md` and `resume.json` before changing copy.

Project skills live in `.cursor/skills/`. Agents must read them at the start of site work and append new learnings there. The always-on rule is `.cursor/rules/skills.mdc`.
